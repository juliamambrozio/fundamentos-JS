const raio = 5.6;
const area = Math.PI + Math.pow(raio,2) //Math: objeto para operaç~eos matemáticas, pow = faz o valor ser elevado o número informado, PI = informa o valor real de PI 

console.log(area.toFixed(2))